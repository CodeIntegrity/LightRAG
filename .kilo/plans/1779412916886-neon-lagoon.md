# 完成 upstream-main 兼容性分析报告

## 背景

`docs/analysis/2026-05-21-upstream-main-compatibility-report.md` 是一份494行的上游合并兼容性分析，但存在以下不足：

1. 报告写于Prompt退役计划之前，未整合退役计划对冲突面的影响
2. 第5节（自动合并但仍需警惕的31个文件）只列文件名，无逐文件风险评估
3. 上游新增模块（chunker/external_parser/native_parser/sidecar/pipeline）只有清单，无架构级依赖分析
4. 缺少 `pyproject.toml` / `package.json` 的依赖分歧深度分析
5. 缺少测试策略——合并后哪些测试保留、迁移、或需改写的判断

## 需新增/扩展的章节

### 新增：第12节 — Prompt退役对冲突面影响的修正分析

- 整合 `docs/aegis/plans/2026-05-21-upstream-main-merge-with-prompt-retirement.md` 的判断
- 逐个修正4.1节中受退役计划影响的冲突分析：
  - `prompt.py`：退役后可直接取上游，冲突面归零
  - `operate.py`：退役本地prompt fingerprint后冲突大幅减少
  - `lightrag.py`：退役后PromptVersionStore相关冲突消失
  - `lightrag_server.py`：退役后prompt_config_routes和active_prompt_versions相关冲突消失
  - WebUI侧：退役后API types冲突减少，stores/state.ts简化

### 扩展：第5节 — 31个自动合并文件的逐文件风险评估

- 按风险等级分组（高/中/低）
- 每类分析：哪些文件涉及存储实现变更可能有语义回归
- `scripts/setup/setup.sh`：上游新增parser/VLM/role config开关
- `lightrag/kg/*_impl.py`：上游对mongo/opensearch/postgres/redis的改动
- `lightrag/api/config.py`：上游新增大量配置项
- `pyproject.toml`：依赖版本分叉分析

### 新增：第13节 — 上游模块架构依赖分析

- `lightrag/pipeline.py` (4486行新增)：文件处理总线的输入输出与接口
- `lightrag/chunker/*`：分块策略与 pipeline 的集成点
- `lightrag/external_parser/*`：Docling/MinerU 外部解析器结构
- `lightrag/native_parser/docx/*`：原生DOCX解析管线
- `lightrag/sidecar/*`：多模态sidecar机制
- `lightrag/parser_routing.py`：解析器路由与chunk_schema
- `lightrag/llm_roles.py`：角色化LLM配置
- `lightrag/multimodal_context.py`：多模态上下文处理
- 各模块与本地代码的交叉点分析

### 新增：第14节 — 测试策略

- 上游新增92个测试文件的分类
- 本地独有测试文件的去留判断（prompt管理相关测试应随退役计划删除）
- 合并后建议的验证路径
- 高危存储回归测试清单

### 扩展：第8节 — 细化的逐文件操作指令

- 将当前的"顺序建议"细化为每个冲突文件的具体操作指令
- 明确每个文件是"取上游"、"保留本地"、"人工拼接"

## 执行步骤

1. 读取上游关键模块的入口文件，理解其架构和集成点
2. 读取上游对存储实现（mongo/opensearch/postgres/redis）的实际改动
3. 读取上游 `pyproject.toml` 依赖变化
4. 整合Prompt退役计划，修正冲突面分析
5. 输出完整的兼容性报告

## 验证

- 报告应覆盖所有16个冲突文件和31个自动合并风险文件
- 报告应包含Prompt退役后的冲突面缩小的量化描述
- 报告应给出可执行的逐文件操作指令
