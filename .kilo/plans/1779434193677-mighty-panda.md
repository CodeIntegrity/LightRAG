# Plan: 验证 API 文档与实际代码一致性

## 目标

检查所有 API 相关文档，验证每个文档记录的 API 是否在代码中实际存在，找出差距并输出一份差异报告。

## 证据来源

- **文档**: `docs/api-support-matrix.md`（核心 API 矩阵）、`docs/LightRAG-API-Server.md`（英文）、`docs/LightRAG-API-Server-zh.md`（中文）、`examples/httpyac-api/`（HTTPYac 示例）
- **代码**: `lightrag/api/routers/`（5 个路由文件）、`lightrag/api/lightrag_server.py`（根路由）

## 代码中实际存在的路由（68 个）

| 模块 | 文件 | 路由数 |
|------|------|--------|
| 文档管理 | `routers/document_routes.py` | 21 |
| 查询 | `routers/query_routes.py` | 4 |
| 图谱 | `routers/graph_routes.py` | 19 |
| 工作空间 | `routers/workspace_routes.py` | 8 |
| Ollama | `routers/ollama_api.py` | 5 |
| 根/鉴权 | `lightrag_server.py` | 9 + 静态文件 |

## 差异分析结果

### 一、文档记录但代码中不存在的 API（严重差异）

#### 1.1 Prompt 版本管理 API（9 个端点）— 已退役

这些路由在提交 `c59ba2e5 retirement(prompt)` 中随 Prompt Management 退役被**显式删除**：
- 删除了 `lightrag/api/routers/prompt_config_routes.py`
- 删除了 `lightrag/prompt_version_store.py`、`lightrag/prompt_versions.py`
- 删除了前端 `lightrag_webui/src/features/PromptManagement.tsx` 等组件
- `prompt_overrides` / `allow_prompt_overrides_via_api` 已从 API 参数中删除
- Dify OpenAPI schema 已同步移除 `prompt_overrides` 字段

退役背景：回归上游 `lightrag/prompt.py` + `prompts/` 文件型定制机制（见 `docs/UPSTREAM-MERGE-SUMMARY.md` 第 429-453 行）。

**缺失的 9 个端点**（在 `api-support-matrix.md` 第 141-151 行标记为"已支持"）：

| 路径 | 方法 | 文档状态 | 实际状态 |
|------|------|----------|----------|
| `/prompt-config/initialize` | POST | 已支持 | **不存在** |
| `/prompt-config/groups` | GET | 已支持 | **不存在** |
| `/prompt-config/{group_type}/versions` | GET | 已支持 | **不存在** |
| `/prompt-config/{group_type}/versions/{version_id}` | GET | 已支持 | **不存在** |
| `/prompt-config/{group_type}/versions` | POST | 已支持 | **不存在** |
| `/prompt-config/{group_type}/versions/{version_id}` | PATCH | 已支持 | **不存在** |
| `/prompt-config/{group_type}/versions/{version_id}/activate` | POST | 已支持 | **不存在** |
| `/prompt-config/{group_type}/versions/{version_id}` | DELETE | 已支持 | **不存在** |
| `/prompt-config/{group_type}/versions/{version_id}/diff` | GET | 已支持 | **不存在** |

**受影响文档文件**：
- `docs/api-support-matrix.md`（第 24、137-151、277、303、310 行）
- `examples/httpyac-api/README.md`（第 10、40、53 行）
- `examples/httpyac-api/05-prompt-config.http`（整个文件引用不存在的 API）
- `docs/superpowers/specs/2026-03-25-workspace-prompt-version-management-design.md`（设计文档，不再适用）

#### 1.2 `GET /documents`（声称已支持/已废弃，但实际不存在）

`api-support-matrix.md` 第 76 行标记为：
> `GET /documents` — 已支持 — 旧接口；已标记为 deprecated，建议使用 `/documents/paginated`

**实际上**：`document_routes.py` 中没有定义任何 `@router.get("")` 或 `@router.get("/")` 路由。该路由从未在代码中实现，或已被移除但文档未更新。

### 二、代码中存在但文档未记录的路由（文档缺口）

| 路径 | 方法 | 代码位置 | 说明 |
|------|------|----------|------|
| `GET /documents/scan-progress` | GET | `document_routes.py:3966` | 扫描进度查询，`api-support-matrix.md` 未收录 |

### 三、受 Prompt 退役影响的 API 参数变更

`api-support-matrix.md` 第 107-109 行记录的 `prompt_overrides` 能力：
> `prompt_overrides` — 部分支持 — 若服务端禁用该能力则返回 403；在 `bypass` 模式下返回 400

该参数已随 Prompt Management 退役被删除。Dify OpenAPI schema (`docs/integrations/dify-query-tool.openapi.json`) 已同步移除该字段，但 `api-support-matrix.md` 仍将其列为支持。

### 四、汇总

| 类别 | 数量 |
|------|------|
| 文档记录但代码中不存在 | **10 个 API**（9 个 prompt-config + 1 个 GET /documents） |
| 代码中存在但文档未记录 | **1 个 API**（GET /documents/scan-progress） |
| 文档记录的能力参数已退役 | **1 个**（prompt_overrides） |
| 引用不存在 API 的文档文件 | **5 个文件** |

## 后续行动

1. 更新 `docs/api-support-matrix.md`：移除 prompt-config 路由表、标记 GET /documents 为不存在、移除 prompt_overrides 能力描述、补入 scan-progress
2. 更新或删除 `examples/httpyac-api/05-prompt-config.http`
3. 更新 `examples/httpyac-api/README.md` 移除对 prompt-config 的引用
4. 评估 `docs/superpowers/specs/2026-03-25-workspace-prompt-version-management-design.md` 是否需要更新或归档
